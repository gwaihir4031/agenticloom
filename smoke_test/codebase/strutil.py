"""String utility module.

The implementation plan (see smoke_test/ticket-code.md) breaks this into
3 tasks, one per function. The foreach smoke pipeline implements each
function in its own iteration; this file accumulates the implementations
across iterations.

Add function implementations below this comment block.
"""
